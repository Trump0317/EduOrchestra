# v0.7 — Planner/Feedback 拆分 + Assistant 智能路由

> 目标：将 Assistant 从「计划 + 反馈 + 公式路由」三合一拆为 Planner / Feedback / Assistant 三个 Agent，全部由 LLM 驱动智能决策。

## 架构变更

```
v0.6                                   v0.7
────                                   ────
assistant(LLM 三合一)                  planner(LLM) ──→ assistant(LLM) ──→ resource → practice
  ↓                                     ↑                                      │
  ├─ 制定计划(首次)                     │                                      ↓
  ├─ 生成反馈(答题后)                    └──────── feedback(LLM) ←──────────────┘
  └─ 公式路由                                       │
                                              assistant(LLM)
                                           读全局状态做智能路由
                                         repeat / next / done
```

- **Planner**（新）：LLM 拆解目标 → 学习计划
- **Feedback**（新）：LLM 分析答题 → 诊断报告（不含路由）
- **Assistant**（重构）：LLM 读全局状态 → 智能路由决策（不靠数学公式）

## 子任务清单

| # | 任务 | 状态 |
|---|------|------|
| v0.7-1 | Planner Agent | [x] |
| v0.7-2 | Feedback Agent | [x] |
| v0.7-3 | Assistant 重构 + 图结构调整 | [ ] |
| v0.7-4 | 测试更新 + 全量回归 | [ ] |

---

## v0.7-1 Planner Agent [ ]

### 目标
新建 Planner Agent，专门负责将学习目标拆解为 2-5 个步骤的学习计划，不再承担反馈和路由职责。

### 依赖
- 无依赖

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `server/orchestrator/agents/planner.py` | Planner 节点 |
| 新建 | `data/prompts/planner.md` | 计划生成 Prompt |

### 实现

**planner.py**：

```python
"""Planner Agent — 将学习目标拆解为步骤序列。"""

from orchestrator.state import AgentState
from orchestrator.llm import llm_invoke_json
from orchestrator.prompt import render_prompt


def planner_node(state: AgentState) -> dict:
    """根据学习目标制定学习计划。"""
    prompt = render_prompt(
        "planner",
        task_goal=state["task_goal"],
    )
    result = llm_invoke_json(prompt)
    # LLM 输出: {"plan": [{"title": "...", "desc": "..."}, ...]}
    return {"plan": result["plan"]}
```

**planner.md** — Prompt 设计：

```
# Role
你是一位经验丰富的高一数学教师，擅长将学习目标拆解为循序渐进的步骤。

# Task
根据学生的学习目标，制定一份 2-5 步的学习计划。每个步骤应具体、可执行，步骤之间由浅入深。

# Input
- 学习目标：{task_goal}

# Output Format
严格输出 JSON：
```json
{{
  "plan": [
    {{"title": "步骤名（10字内）", "desc": "具体学习内容和标准（40字内）"}}
  ]
}}
```

# Constraints
- 2-5 个步骤
- 每个步骤有明确的学习产出标准
- 由浅入深：概念理解 → 方法掌握 → 综合应用
- title 10 字以内，desc 40 字以内
- 不要输出 JSON 之外的文字
```

### 测试
- 调真实 LLM：验证返回 plan 含 2-5 个步骤，每个步骤含 title/desc

### 验收标准
- [ ] `from orchestrator.agents.planner import planner_node` 无报错
- [ ] planner_node 返回 `{"plan": [...]}`，plan 含 2-5 个步骤
- [ ] 每个步骤含 title（非空字符串）和 desc（非空字符串）

---

## v0.7-2 Feedback Agent [ ]

### 目标
新建 Feedback Agent，专门分析学生的答题表现并生成诊断报告。只输出反馈内容，不做路由决策。

### 依赖
- v0.7-1（Planner 就绪，但 Feedback 可独立开发测试）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `server/orchestrator/agents/feedback.py` | Feedback 节点 |
| 新建 | `data/prompts/feedback.md` | 答题分析 Prompt |

### 实现

**feedback.py**：

```python
"""Feedback Agent — 分析答题表现，生成诊断报告。"""

import json
from orchestrator.state import AgentState
from orchestrator.llm import llm_invoke_json
from orchestrator.prompt import render_prompt


def feedback_node(state: AgentState) -> dict:
    """分析学生本轮答题表现，生成诊断反馈。"""
    step = state["plan"][state["current_step"]]
    answers = state.get("answers", [])
    history = state.get("step_history", [])

    # 本轮统计
    total_q = len(answers)
    correct_q = sum(1 for a in answers if a.get("is_correct"))
    accuracy = correct_q / total_q if total_q > 0 else 0

    # 本步历史
    step_records = [h for h in history if h.get("step_index") == state["current_step"]]
    current_rounds = len(step_records) + 1
    prev_best = max([h.get("best_accuracy", 0) for h in step_records], default=0)

    prompt = render_prompt(
        "feedback",
        task_goal=state["task_goal"],
        step_title=step["title"],
        step_desc=step["desc"],
        total_questions=total_q,
        correct_count=correct_q,
        accuracy=f"{accuracy:.0%}",
        current_rounds=current_rounds,
        prev_best=f"{prev_best:.0%}",
        answers_detail=_format_answers(answers),
        history_text=_format_history(step_records),
    )
    result = llm_invoke_json(prompt)
    # LLM 输出: {"feedback": {"summary": "...", "strengths": [...], "weaknesses": [...], "suggestion": "..."}}
    return {"feedback": result["feedback"]}


def _format_answers(answers: list[dict]) -> str:
    if not answers:
        return "无答题记录"
    lines = []
    for i, a in enumerate(answers):
        status = "✅" if a.get("is_correct") else "❌"
        lines.append(
            f"  {status} 题{i + 1}: 学生选 {a.get('student_answer', '?')}"
            f"（正确: {a.get('correct_answer', '?')}）"
        )
    return "\n".join(lines)


def _format_history(records: list[dict]) -> str:
    if not records:
        return "无历史记录"
    lines = []
    for r in records:
        lines.append(
            f"  第{r.get('rounds', '?')}轮: "
            f"正确率 {r.get('latest_accuracy', 0):.0%}"
        )
    return "\n".join(lines)
```

**feedback.md** — Prompt 设计：

```
# Role
你是一位善于诊断学情的高一数学教师。

# Task
分析学生本轮答题表现，诊断掌握情况和薄弱点，给出针对性建议。

# Input
- 学习目标：{task_goal}
- 当前步骤：{step_title}
- 步骤描述：{step_desc}
- 题目总数：{total_questions}
- 正确数：{correct_count}
- 正确率：{accuracy}
- 本轮是第 {current_rounds} 轮学习本步骤
- 本步历史最佳正确率：{prev_best}

- 答题详情：
{answers_detail}

- 本步历史记录：
{history_text}

# Output Format
严格输出 JSON：
```json
{{
  "feedback": {{
    "summary": "整体评价（20-40字）",
    "strengths": ["掌握较好的点"],
    "weaknesses": ["需要加强的点"],
    "suggestion": "具体可操作的学习建议（20-40字）"
  }}
}}
```

# Constraints
- summary 从学生角度出发，客观描述表现
- strengths 和 weaknesses 各 1-3 条
- suggestion 具体到可以立即执行
- 如正确率相比历史有进步，在 summary 中给予鼓励
- 不要输出 JSON 之外的文字
```

### 测试
- 调真实 LLM：验证返回 feedback 含 summary/strengths/weaknesses/suggestion

### 验收标准
- [ ] `from orchestrator.agents.feedback import feedback_node` 无报错
- [ ] feedback_node 返回 `{"feedback": {...}}`，含 summary/strengths/weaknesses/suggestion
- [ ] summary 非空字符串，strengths 和 weaknesses 为 list[str]

---

## v0.7-3 Assistant 重构 + 图结构调整 [ ]

### 目标
将 Assistant 从「计划+反馈+公式路由」三合一改为纯 LLM 智能路由器，读取全局状态做路由决策。同时更新 LangGraph 图结构，加入 Planner 和 Feedback 节点。

### 依赖
- v0.7-1（Planner）
- v0.7-2（Feedback）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `server/orchestrator/agents/assistant.py` | 重写为 LLM 智能路由器 |
| 修改 | `data/prompts/assistant.md` | 重写为路由决策 Prompt |
| 修改 | `server/orchestrator/graph.py` | 新图结构：planner/assistant/feedback 五节点 |
| 修改 | `server/orchestrator/state.py` | Assistant 输出增加 decision_reason 字段（可选） |

### 实现

**assistant.py** — 纯 LLM 路由器：

```python
"""Assistant Agent — LLM 智能路由器。

读取全局状态（plan + feedback + step_history + answers），
由 LLM 做出有教学意义的路由决策。
"""

import json

from orchestrator.state import AgentState
from orchestrator.llm import llm_invoke_json
from orchestrator.prompt import render_prompt


def assistant_node(state: AgentState) -> dict:
    """LLM 分析全局状态，决定下一步：repeat / next / done。

    被调用两次：
    1. Planner 之后（无 feedback）→ 决定开始学习
    2. Feedback 之后（有 feedback）→ 智能路由
    """
    plan = state.get("plan", [])
    step_index = state.get("current_step", 0)
    total_steps = len(plan)
    feedback = state.get("feedback")
    answers = state.get("answers", [])
    history = state.get("step_history", [])

    step = plan[step_index] if step_index < total_steps else None
    step_records = [h for h in history if h.get("step_index") == step_index]

    # 渲染 Prompt（首次 / 答题后使用同一份 Prompt，由 LLM 自行判断）
    prompt = render_prompt(
        "assistant",
        task_goal=state["task_goal"],
        total_steps=total_steps,
        current_step=step_index + 1,
        step_title=step["title"] if step else "（无）",
        step_desc=step.get("desc", "") if step else "",
        plan_summary=json.dumps(
            [f"{i+1}. {s['title']}" for i, s in enumerate(plan)],
            ensure_ascii=False,
        ),
        has_feedback="是" if feedback else "否",
        feedback_text=json.dumps(feedback, ensure_ascii=False) if feedback else "无（首次启动）",
        step_history_text=_format_step_history(step_records),
        answers_text=_format_answers(answers),
        current_rounds=len(step_records) + 1 if step else 0,
    )
    result = llm_invoke_json(prompt)
    # LLM 输出: {"action": "next"|"repeat"|"done", "reason": "决策理由"}

    update: dict = {
        "next_action": result["action"],
    }

    # 更新 step_history
    if answers:
        total_q = len(answers)
        correct_q = sum(1 for a in answers if a.get("is_correct"))
        accuracy = correct_q / total_q if total_q > 0 else 0
        prev_best = max(
            [h.get("best_accuracy", 0) for h in step_records], default=0
        )
        new_record = {
            "step_index": step_index,
            "rounds": len(step_records) + 1,
            "best_accuracy": max(accuracy, prev_best),
            "latest_accuracy": accuracy,
        }
        filtered = [h for h in history if h.get("step_index") != step_index]
        update["step_history"] = filtered + [new_record]

    # 步骤推进
    if result["action"] == "next":
        update["current_step"] = step_index + 1

    return update


def _format_answers(answers: list[dict]) -> str:
    if not answers:
        return "无答题记录"
    lines = []
    for i, a in enumerate(answers):
        status = "✅" if a.get("is_correct") else "❌"
        lines.append(
            f"  {status} 题{i + 1}: 学生选 {a.get('student_answer', '?')}"
            f"（正确: {a.get('correct_answer', '?')}）"
        )
    return "\n".join(lines)


def _format_step_history(records: list[dict]) -> str:
    if not records:
        return "首次学习本步骤"
    lines = []
    for r in records:
        lines.append(
            f"  第{r.get('rounds', '?')}轮: "
            f"正确率 {r.get('latest_accuracy', 0):.0%}"
        )
    return "\n".join(lines)
```

**assistant.md** — 路由决策 Prompt：

```
# Role
你是 EduOrchestra 的学习主管，负责根据全局学习状态做出路由决策。

# Context
- 学习目标：{task_goal}
- 总步骤数：{total_steps}
- 当前位置：第 {current_step} / {total_steps} 步「{step_title}」
- 步骤描述：{step_desc}
- 计划总览：{plan_summary}
- 是否有诊断反馈：{has_feedback}
- 诊断反馈内容：{feedback_text}
- 本步历史：{step_history_text}
- 本轮答题：{answers_text}
- 本步已学轮数：{current_rounds}

# Decision Guidelines
请综合以下因素做出教学判断：

1. **答题表现**：正确率反映掌握程度
2. **诊断反馈**：feedback 中的 strengths/weaknesses 提示薄弱点
3. **学习历史**：反复学习但无进步 → 可能是方法问题，可尝试下一轮或换步骤
4. **进度节奏**：不要让学生在一处卡太久，但也不能匆匆跳过未掌握的内容

# Decision Options
- **next**：进入下一步。适用于：掌握良好，或经多次尝试建议换个角度
- **repeat**：重新学习当前步骤。适用于：尚未掌握，且有改进空间
- **done**：全部完成。适用于：最后一步已掌握

# Output Format
严格输出 JSON：
```json
{{
  "action": "next" | "repeat" | "done",
  "reason": "一句话说明决策理由（20字内）"
}}
```

# Constraints
- 首次调用（无 feedback）→ action 必须是 "next"
- 结合 feedback.weaknesses 和答题正确率做判断
- 同一步骤学习 3 轮以上仍不理想 → 倾向 "next"（不死循环）
- reason 要能体现教学考量，不是机械判断
- 不要输出 JSON 之外的文字
```

**graph.py** — 新图结构：

```python
from langgraph.graph import StateGraph, END
from langgraph.checkpoint.memory import MemorySaver

from orchestrator.state import AgentState
from orchestrator.agents.planner import planner_node
from orchestrator.agents.assistant import assistant_node
from orchestrator.agents.resource import resource_node
from orchestrator.agents.practice import practice_node
from orchestrator.agents.feedback import feedback_node


def decide_route(state: AgentState) -> str:
    """根据 Assistant 的 next_action 路由。"""
    action = state.get("next_action", "")
    if action == "done":
        return END
    return "resource"


def build_graph() -> StateGraph:
    graph = StateGraph(AgentState)

    graph.add_node("planner", planner_node)
    graph.add_node("assistant", assistant_node)
    graph.add_node("resource", resource_node)
    graph.add_node("practice", practice_node)
    graph.add_node("feedback", feedback_node)

    graph.set_entry_point("planner")

    # planner → assistant（首次路由决策）
    graph.add_edge("planner", "assistant")

    # assistant → resource 或 END
    graph.add_conditional_edges("assistant", decide_route)

    # resource → practice
    graph.add_edge("resource", "practice")

    # practice → feedback（答题后诊断）
    graph.add_edge("practice", "feedback")

    # feedback → assistant（智能路由）
    graph.add_edge("feedback", "assistant")

    return graph.compile(
        checkpointer=MemorySaver(),
        interrupt_after=["practice"],
    )
```

**state.py** — 小改动：assistant_node 的 next_action 字段已有，无需新增类型。feedback 字段已存在。

### 测试
- 单元测试：图结构断言包含 planner/assistant/resource/practice/feedback 五个节点
- 集成测试：demo.py 完整链路跑通

### 验收标准
- [ ] 图形包含 5 个节点：planner / assistant / resource / practice / feedback
- [ ] planner → assistant → resource → practice → feedback → assistant 边链正确
- [ ] demo.py 完整链路跑通（planner → 看计划 → resource → 做题 → feedback → assistant 路由）
- [ ] Assistant 的 decision_reason 非空

---

## v0.7-4 测试更新 + 全量回归 [ ]

### 目标
更新测试适配新架构，确保全量通过。

### 依赖
- v0.7-3（图结构 + Assistant 重构完成）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `server/tests/unit/test_graph_structure.py` | 图结构断言改为 5 节点 |
| 新建 | `server/tests/integration/test_planner.py` | Planner 集成测试 |
| 新建 | `server/tests/integration/test_feedback.py` | Feedback 集成测试 |
| 修改 | `server/tests/integration/test_assistant.py` | 适配新 assistant 行为 |
| 修改 | `server/tests/integration/test_workflow.py` | 新图流转路径 |
| 修改 | `server/tests/integration/test_task_api.py` | API 响应适配 |
| 修改 | `server/routers/task.py` | 创建任务初始状态适配（如有需要） |

### 验收标准
- [ ] `python -m pytest server/tests/unit/ -v` 全部通过
- [ ] `python -m pytest server/tests/integration/ -v` 全部通过
- [ ] 无回归（所有已有功能正常）

---

## v0.7 版本验收

### 整体验收
- [ ] 全量 pytest 通过，零失败
- [ ] 浏览器手动走通全流程：输入目标 → 看计划 → 看资料 → 做题 → 看反馈 → 看路由决策
- [ ] Assistant 路由决策包含 reason（非公式化）
- [ ] Planner / Feedback / Assistant 三个 LLM 调用各司其职
- [ ] demo.py 完整链路可跑通
- [ ] 前端正常展示（无破坏性变更）

### 不包含
- SQLite 持久化
- Chat Agent
- 题库扩展 / AI 题目生成
- 用户认证
- 知识图谱
