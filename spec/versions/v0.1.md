# v0.1 - 工程骨架

> 目标：搭建项目骨架，FastAPI 空服务 + React 空前端均可启动，LLM 配置就绪

## 子任务清单

| # | 任务 | 状态 |
|---|------|------|
| v0.1-1 | 项目初始化 + 目录结构 + 依赖管理 | [x] |
| v0.1-2 | FastAPI 空服务（health 端点） | [ ] |
| v0.1-3 | 配置管理（.env 读取） | [ ] |
| v0.1-4 | React 空前端（Vite + 标题展示） | [ ] |
| v0.1-5 | 配置 API（GET/PUT /api/config） | [ ] |

---

## v0.1-1 项目初始化 + 目录结构 [ ]

### 目标
创建完整的项目目录结构，初始化 Python 和 Node.js 依赖管理文件。

### 依赖
- 无

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `pyproject.toml` | Python 项目元数据 + 依赖声明 |
| 新建 | `requirements.txt` | 精确依赖锁定（FastAPI、uvicorn、openai、langgraph 等） |
| 新建 | `.env.example` | 环境变量模板（LLM_PROVIDER、LLM_API_KEY、LLM_MODEL） |
| 新建 | `.gitignore` | Python + Node.js + IDE 忽略规则 |
| 新建 | `README.md` | 项目简介 + 快速启动指南 |
| 新建 | `server/__init__.py` | Python 包标记 |
| 新建 | `server/tests/__init__.py` | 测试包标记 |
| 新建 | `server/tests/unit/__init__.py` | 单元测试包标记 |
| 新建 | `server/tests/integration/__init__.py` | 集成测试包标记 |
| 新建 | `client/` | React 项目（v0.1-4 填充） |
| 新建 | `data/` | 运行时数据目录 |
| 新建 | `data/mock/` | Mock 数据目录（v0.2 起使用） |
| 新建 | `data/.gitkeep` | 保持空目录入 git |

### 实现

```toml
# pyproject.toml
[project]
name = "eduorchestra"
version = "0.1.0"
description = "多智能体驱动的 K12 数学自主学习系统"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "openai>=1.55.0",
    "langgraph>=0.2.0",
    "langchain>=0.3.0",
    "sqlalchemy>=2.0.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.28.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0",
]
```

```bash
# requirements.txt (pip freeze 风格，精确版本)
fastapi==0.115.*
uvicorn[standard]==0.32.*
openai==1.55.*
langgraph==0.2.*
langchain==0.3.*
sqlalchemy==2.0.*
python-dotenv==1.0.*
httpx==0.28.*
```

```bash
# .env.example
LLM_PROVIDER=deepseek
LLM_API_KEY=sk-your-api-key-here
LLM_MODEL=deepseek-chat
```

```markdown
<!-- README.md -->
# EduOrchestra

多智能体驱动的 K12 数学自主学习系统。

## 快速启动

### 后端
cd server
cp ../.env.example ../.env  # 编辑 .env 填入 API Key
uvicorn main:app --reload

### 前端
cd client
npm install
npm run dev
```

### 测试
- 无自动化测试（纯目录结构 + 配置文件创建）
- 手动验证：`pip install -r requirements.txt` 无报错，`python -c "import fastapi; print(fastapi.__version__)"` 正常

### 验收标准
- [ ] `pyproject.toml` 和 `requirements.txt` 可正常 `pip install`
- [ ] `.env.example` 包含 LLM_PROVIDER、LLM_API_KEY、LLM_MODEL 三个变量
- [ ] `.gitignore` 忽略 `.env`、`__pycache__`、`node_modules`、`*.db`
- [ ] `README.md` 包含项目简介和快速启动步骤
- [ ] 目录结构：`server/`、`client/`、`data/`、`data/mock/`、`spec/` 均已存在

---

## v0.1-2 FastAPI 空服务 [ ]

### 目标
创建 FastAPI 应用入口，提供 `/api/health` 健康检查端点。

### 依赖
- v0.1-1（目录结构和 pyproject.toml）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `server/main.py` | FastAPI 应用入口，注册路由，CORS 配置 |

### 实现

```python
# server/main.py
import os
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(
    title="EduOrchestra",
    description="多智能体驱动的 K12 数学自主学习系统",
    version="0.1.0",
)

# CORS: 允许前端开发服务器跨域
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)


@app.get("/api/health")
def health():
    return {"status": "ok", "version": "0.1.0"}


if __name__ == "__main__":
    import uvicorn
    uvicorn.run("main:app", host="0.0.0.0", port=8000, reload=True)
```

### 测试

```python
# server/tests/integration/test_health.py
# 测试行为：
# - test_health_returns_ok: GET /api/health 返回 200 + body 含 status:ok 和 version
import pytest
from httpx import AsyncClient, ASGITransport
from main import app


@pytest.mark.asyncio
async def test_health_returns_ok():
    async with AsyncClient(
        transport=ASGITransport(app=app), base_url="http://test"
    ) as client:
        resp = await client.get("/api/health")
        assert resp.status_code == 200
        data = resp.json()
        assert data["status"] == "ok"
        assert "version" in data
```

### 验收标准
- [ ] `uvicorn main:app --reload` 启动后端，访问 `http://localhost:8000/api/health` 返回 `{"status": "ok", "version": "0.1.0"}`
- [ ] `pytest server/tests/integration/test_health.py -v` 测试通过
- [ ] FastAPI 自动生成的 OpenAPI docs 可访问 `http://localhost:8000/docs`

---

## v0.1-3 配置管理 [ ]

### 目标
从 `.env` 读取 LLM 配置（Provider、API Key、Model），提供统一配置访问入口。

### 依赖
- v0.1-1（.env.example 已创建）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `server/config.py` | 配置类，读取 `.env` + 环境变量 |

### 实现

```python
# server/config.py
import os
from dotenv import load_dotenv

load_dotenv()


class Config:
    LLM_PROVIDER: str = os.getenv("LLM_PROVIDER", "deepseek")
    LLM_API_KEY: str = os.getenv("LLM_API_KEY", "")
    LLM_MODEL: str = os.getenv("LLM_MODEL", "deepseek-chat")

    @classmethod
    def validate(cls) -> list[str]:
        """校验必要配置，返回缺失项列表"""
        missing = []
        if not cls.LLM_API_KEY:
            missing.append("LLM_API_KEY")
        return missing

    @classmethod
    def dict(cls) -> dict:
        return {
            "provider": cls.LLM_PROVIDER,
            "model": cls.LLM_MODEL,
            "has_api_key": bool(cls.LLM_API_KEY),
        }


config = Config()
```

### 测试

```python
# server/tests/unit/test_config.py
# 测试行为：
# - test_config_defaults: 无环境变量时使用默认值
# - test_config_validates_missing_key: 缺 API Key 时 validate() 返回缺失列表
# - test_config_dict: dict() 返回 provider/model/has_api_key，不暴露 api_key
import os
import pytest
from unittest.mock import patch
from config import Config


def test_config_defaults():
    with patch.dict(os.environ, {}, clear=True):
        # 无环境变量时使用默认值
        from importlib import reload
        import config
        reload(config)
        assert config.config.LLM_PROVIDER == "deepseek"


def test_config_validates_missing_key():
    with patch.dict(os.environ, {"LLM_API_KEY": ""}, clear=True):
        from importlib import reload
        import config
        reload(config)
        assert "LLM_API_KEY" in config.config.validate()


def test_config_dict():
    with patch.dict(os.environ, {
        "LLM_PROVIDER": "openai",
        "LLM_API_KEY": "sk-test",
        "LLM_MODEL": "gpt-4o",
    }, clear=True):
        from importlib import reload
        import config
        reload(config)
        d = config.config.dict()
        assert d["provider"] == "openai"
        assert d["has_api_key"] is True
        # API Key 不应暴露在 dict 中
        assert "api_key" not in d
```

### 验收标准
- [ ] `.env` 中配置 `LLM_API_KEY` 后，`config.LLM_API_KEY` 可读取
- [ ] `config.validate()` 正确检测缺失项
- [ ] `config.dict()` 返回 provider/model/has_api_key，不暴露 API Key
- [ ] `pytest server/tests/unit/test_config.py -v` 全部通过

---

## v0.1-4 React 空前端 [ ]

### 目标
用 Vite + React + TypeScript 初始化前端项目，显示 "EduOrchestra" 标题。

### 依赖
- 无（独立于后端）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `client/package.json` | Node.js 依赖（react, vite, typescript） |
| 新建 | `client/vite.config.ts` | Vite 配置 + 代理设置 |
| 新建 | `client/tsconfig.json` | TypeScript 配置 |
| 新建 | `client/index.html` | HTML 入口 |
| 新建 | `client/src/main.tsx` | React 入口 |
| 新建 | `client/src/App.tsx` | 根组件 |
| 新建 | `client/src/App.css` | 基础样式 |

### 实现

```typescript
// client/vite.config.ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      "/api": {
        target: "http://localhost:8000",
        changeOrigin: true,
      },
    },
  },
});
```

```tsx
// client/src/App.tsx
import { useState, useEffect } from "react";
import "./App.css";

function App() {
  const [backendStatus, setBackendStatus] = useState<string>("checking...");

  useEffect(() => {
    fetch("/api/health")
      .then((res) => res.json())
      .then((data) => setBackendStatus(data.status))
      .catch(() => setBackendStatus("unreachable"));
  }, []);

  return (
    <div className="app">
      <h1>EduOrchestra</h1>
      <p>多智能体驱动的 K12 数学自主学习系统</p>
      <p className="status">
        Backend: <span className={backendStatus === "ok" ? "ok" : "error"}>{backendStatus}</span>
      </p>
    </div>
  );
}

export default App;
```

### 测试
- 无自动化测试
- 手动验证：`npm run dev` 后浏览器打开 `localhost:5173`，页面显示 "EduOrchestra" 和 Backend 状态

### 验收标准
- [ ] `cd client && npm install && npm run dev` 正常启动
- [ ] 浏览器访问 `localhost:5173` 显示 "EduOrchestra" 标题
- [ ] 后端启动时，前端显示 `Backend: ok`（通过 Vite proxy 调用 `/api/health`）
- [ ] 后端未启动时，前端显示 `Backend: unreachable`

---

## v0.1-5 配置 API [ ]

### 目标
提供 `/api/config` 端点，读取和更新 LLM 配置，前端可展示配置状态。

### 依赖
- v0.1-2（FastAPI 服务）
- v0.1-3（配置管理）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `server/routers/__init__.py` | 路由包标记 |
| 新建 | `server/routers/config.py` | 配置路由 |
| 修改 | `server/main.py` | 注册 config 路由 |

### 实现

```python
# server/routers/config.py
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel
from config import config as app_config

router = APIRouter(prefix="/api/config", tags=["config"])


class ConfigResponse(BaseModel):
    provider: str
    model: str
    has_api_key: bool


class ConfigUpdate(BaseModel):
    provider: str | None = None
    api_key: str | None = None
    model: str | None = None


@router.get("")
def get_config() -> ConfigResponse:
    return ConfigResponse(**app_config.dict())


@router.put("")
def update_config(body: ConfigUpdate):
    """更新运行内存中的配置（不写入 .env 文件）"""
    if body.provider:
        app_config.LLM_PROVIDER = body.provider
    if body.api_key:
        app_config.LLM_API_KEY = body.api_key
    if body.model:
        app_config.LLM_MODEL = body.model
    return ConfigResponse(**app_config.dict())
```

```python
# server/main.py (追加)
from routers import config

app.include_router(config.router)
```

### 测试

```python
# server/tests/integration/test_config_api.py
# 测试行为：
# - test_get_config: GET /api/config 返回 200 + 含 provider/model/has_api_key 不含 api_key
# - test_update_config: PUT /api/config 更新 model 后，GET 返回更新后的值
import pytest
from httpx import AsyncClient, ASGITransport
from main import app


@pytest.mark.asyncio
async def test_get_config():
    async with AsyncClient(
        transport=ASGITransport(app=app), base_url="http://test"
    ) as client:
        resp = await client.get("/api/config")
        assert resp.status_code == 200
        data = resp.json()
        assert "provider" in data
        assert "model" in data
        assert "has_api_key" in data
        # api_key 不应泄漏
        assert "api_key" not in data


@pytest.mark.asyncio
async def test_update_config():
    async with AsyncClient(
        transport=ASGITransport(app=app), base_url="http://test"
    ) as client:
        resp = await client.put("/api/config", json={"model": "gpt-4o"})
        assert resp.status_code == 200
        data = resp.json()
        assert data["model"] == "gpt-4o"
```

### 验收标准
- [ ] `GET /api/config` 返回 provider、model、has_api_key（不暴露 api_key）
- [ ] `PUT /api/config` 可更新运行内存中的配置
- [ ] `pytest server/tests/integration/test_config_api.py -v` 全部通过

---

## v0.1 版本验收

### 整体验收

- [ ] `cd server && uvicorn main:app --reload` 正常启动
- [ ] `cd client && npm run dev` 正常启动
- [ ] 浏览器 `localhost:5173` 显示 "EduOrchestra" + Backend 状态为 `ok`
- [ ] `GET /api/health` 返回 `{"status": "ok"}`
- [ ] `GET /api/config` 返回当前 LLM 配置
- [ ] v0.1-1 无测试要求；v0.1-2~v0.1-5 各自的 pytest 测试通过
- [ ] `curl http://localhost:8000/docs` 可访问 OpenAPI 文档

### 不包含

- LLM 调用（v0.2 引入）
- 数据库（v0.9 引入）
- 知识图谱（v0.2 引入）
- 任何业务逻辑
