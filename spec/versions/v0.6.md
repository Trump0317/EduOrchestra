# v0.6 — 资源获取优化：真实搜索 + LLM 富化

> 目标：Resource Agent 从 LLM 幻觉 URL 升级为 DuckDuckGo 真实搜索 + LLM 过滤富化，返回可点击的真实学习资料。

## 架构变更

```
v0.5                               v0.6
────                               ────
resource_node:                     resource_node:
  step → LLM 编 URL                  step → 构建搜索词
       → 返回 type/title/url              → DuckDuckGo 搜索 → 真实 URL
       (URL 不可用)                       → LLM 过滤富化 → 结构化资源
                                          → 返回 type/title/url/description/knowledge_points
```

- 新增 DuckDuckGo 搜索工具层 `tools.py`
- ResourceEntry 类型扩展（增加 description、knowledge_points）
- Resource Agent 改为两步：搜索 → LLM 富化
- 前端 Resource 页面展示知识点标签和资料描述

## 子任务清单

| # | 任务 | 状态 |
|---|------|------|
| v0.6-1 | 搜索工具 + State 扩展 | [x] |
| v0.6-2 | Resource Agent 重构 | [ ] |
| v0.6-3 | 前端 Resource 页面升级 | [ ] |
| v0.6-4 | 测试更新 + 全量回归 | [ ] |

---

## v0.6-1 搜索工具 + State 扩展 [ ]

### 目标
新建 DuckDuckGo 搜索工具模块，扩展 ResourceEntry 类型增加 description 和 knowledge_points，作为 v0.6-2 的基础设施。

### 依赖
- 无依赖

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 新建 | `server/orchestrator/tools.py` | DuckDuckGo 搜索封装 + 查询词构建 |
| 修改 | `server/orchestrator/state.py` | ResourceEntry 增加 description, knowledge_points |
| 修改 | `requirements.txt` | 增加 duckduckgo_search |
| 修改 | `pyproject.toml` | 增加 duckduckgo_search 依赖 |

### 实现

**tools.py** — 搜索封装：

```python
"""外部工具 — DuckDuckGo 搜索。

为 Resource Agent 提供真实网络搜索能力。
"""

from duckduckgo_search import DDGS


def search_resources(query: str, max_results: int = 10) -> list[dict]:
    """搜索中文学习资源。

    Args:
        query: 搜索关键词
        max_results: 最大结果数

    Returns:
        [{"title": "...", "url": "...", "snippet": "..."}, ...]
        搜索失败时返回空列表（不抛异常，上游降级处理）
    """
    try:
        with DDGS() as ddgs:
            results = list(ddgs.text(
                query,
                region="cn-zh",
                max_results=max_results,
            ))
        return [
            {"title": r["title"], "url": r["href"], "snippet": r["body"]}
            for r in results
        ]
    except Exception:
        # DuckDuckGo 偶发限流或网络异常，返回空列表让上游降级
        return []


def build_search_query(step_title: str, step_desc: str) -> str:
    """根据步骤信息构建中文搜索查询词。

    拼接为 "{step_title} {step_desc} 高一数学"，
    限制总长 80 字符以内。
    """
    base = f"{step_title} {step_desc} 高一数学"
    if len(base) > 80:
        base = base[:77] + "..."
    return base
```

**state.py** — ResourceEntry 扩展：

```python
class ResourceEntry(TypedDict):
    type: str                 # "video" | "article"
    title: str
    url: str
    description: str          # 新增 v0.6：资料简介（30-80字）
    knowledge_points: list[str]  # 新增 v0.6：关联知识点（1-3个）
```

已有字段 `type`、`title`、`url` 保持不变，向下兼容。

### 测试
- `search_resources("二次函数")` 返回 list，每条含 title/url/snippet
- 网络不可用时返回空列表，不抛异常
- `build_search_query` 超 80 字符时截断

### 验收标准
- [ ] `from orchestrator.tools import search_resources, build_search_query` 无报错
- [ ] `search_resources("二次函数")` 返回 list[dict]，每条含 title/url/snippet
- [ ] 无网络时 search_resources 返回 [] 不抛异常
- [ ] build_search_query 返回值 ≤ 80 字符
- [ ] ResourceEntry 类型含 description、knowledge_points 字段
- [ ] `pip install duckduckgo_search` 无版本冲突

---

## v0.6-2 Resource Agent 重构 [ ]

### 目标
将 Resource Agent 从「LLM 直接编 URL」改为「DuckDuckGo 搜索 → LLM 过滤富化」两步流程，返回含真实 URL、描述、知识点标签的结构化资源。

### 依赖
- v0.6-1（搜索工具 + State 扩展就绪）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `server/orchestrator/agents/resource.py` | 新两步流程 |
| 修改 | `data/prompts/resource.md` | 新 Prompt：输入含搜索结果，输出富化资源 |

### 实现

**resource.py** — 核心流程（伪代码）：

```
def resource_node(state: AgentState) -> dict:
    step = state["plan"][state["current_step"]]

    # Step 1: 构建搜索词 + 搜索
    query = build_search_query(step["title"], step["desc"])
    search_results = search_resources(query, max_results=10)

    # Step 2: LLM 过滤 + 富化 + 结构化
    prompt = render_prompt(
        "resource",
        task_goal=state["task_goal"],
        step_title=step["title"],
        step_desc=step["desc"],
        search_results_text=format_search_results(search_results),
    )
    result = llm_invoke_json(prompt)
    # LLM 输出: {"resources": [{"type": "video"|"article", "title": "...",
    #   "url": "...", "description": "...", "knowledge_points": [...]}, ...]}

    return {"resources": result["resources"]}
```

**format_search_results 辅助函数**（同文件内）：

将 search_results 格式化为 LLM prompt 可读的文本列表，每条含编号、标题、URL、摘要片段。

```python
def _format_search_results(results: list[dict]) -> str:
    """将搜索结果格式化为 LLM 可读文本。"""
    if not results:
        return "（无搜索结果，请根据步骤信息自行推荐 2-3 条学习资料）"
    lines = []
    for i, r in enumerate(results, 1):
        lines.append(f"{i}. {r['title']}")
        lines.append(f"   URL: {r['url']}")
        lines.append(f"   摘要: {r['snippet'][:100]}")
    return "\n".join(lines)
```

**降级策略**：搜索结果为空时，Prompt 告知 LLM 自行推荐，此时行为退化回当前 v0.5 模式（LLM 编 URL），但输出结构仍然是新版（含 description、knowledge_points）。

**resource.md** — 新 Prompt：

```
# Role
你是一位擅长筛选和推荐教育资源的高一数学教师。

# Task
根据搜索结果，筛选出与当前学习步骤最相关的 2-5 条高质量学习资料。为每条资料补充描述、标注知识点。

# Input
- 学习总目标：{task_goal}
- 当前步骤标题：{step_title}
- 当前步骤描述：{step_desc}
- 搜索结果：
{search_results_text}

# Output Format
严格输出 JSON，不要其他文字：
```json
{{
  "resources": [
    {{
      "type": "video",
      "title": "资料标题（从搜索结果中选取，保持原标题）",
      "url": "https://xxxxx（从搜索结果中选取真实 URL）",
      "description": "用 30-80 字概括该资料的核心内容，帮助学生判断是否适合自己",
      "knowledge_points": ["知识点1", "知识点2"]
    }}
  ]
}}
```

# Constraints
- type 只能是 "video" 或 "article"
  - Bilibili / YouTube 等视频平台 → "video"
  - 其他网页 → "article"
- title 优先使用搜索结果中的原标题
- url 必须来自搜索结果，不得编造
- description 30-80 字，客观描述资料内容
- knowledge_points 1-3 个，从步骤描述中提炼
- 推荐 2-5 条资料
- 搜索结果不足时，可从步骤信息推断推荐 2-3 条，此时 url 使用搜索结果中相关的或留空
- 不要输出 JSON 之外的任何文字
```

### 测试
- 调真实 LLM + 真实搜索：验证返回 resources 含 description 和 knowledge_points
- URL 至少包含一个 http 开头
- 搜索结果为空时降级不崩溃

### 验收标准
- [ ] resource_node 返回 resources 列表，每条含 type/title/url/description/knowledge_points
- [ ] url 字段至少有一条以 http 开头
- [ ] description 字段非空字符串
- [ ] knowledge_points 字段为 list[str] 且非空
- [ ] 搜索结果为空时 resource_node 正常返回（降级模式）
- [ ] demo.py 跑通完整链路（assistant → resource → practice）

---

## v0.6-3 前端 Resource 页面升级 [ ]

### 目标
前端 Resource 页面展示资料描述和知识点标签，替代原来只显示 type 图标 + 标题的简单布局。

### 依赖
- v0.6-2（Resource Agent 返回新字段）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `client/src/App.tsx` | ResourceView 展示 description + knowledge_points |
| 修改 | `client/src/App.css` | 知识点标签样式、描述样式 |

### 实现

**TaskState 类型扩展**（App.tsx 内）：

```typescript
interface TaskState {
  // ... 现有字段不变
  resources: {
    type: string;
    title: string;
    url: string;
    description: string;         // 新增
    knowledge_points: string[];  // 新增
  }[];
}
```

**ResourceView 组件改动**：

```
每条资料卡片从:
  🎬 资料标题
改为:
  🎬 资料标题
  简介: description（灰色小字）
  [知识点1] [知识点2]（标签样式）
```

```tsx
{taskState.resources.map((r, i) => (
  <a key={i} href={r.url} target="_blank" rel="noopener noreferrer" className="res-item">
    <span className="res-icon">{r.type === "video" ? "🎬" : "📄"}</span>
    <div className="res-content">
      <span className="res-title">{r.title}</span>
      {r.description && <span className="res-desc">{r.description}</span>}
      {r.knowledge_points && r.knowledge_points.length > 0 && (
        <div className="res-kps">
          {r.knowledge_points.map((kp, j) => (
            <span key={j} className="kp-tag">{kp}</span>
          ))}
        </div>
      )}
    </div>
  </a>
))}
```

**CSS 新增样式**：

```css
.res-content { flex: 1; display: flex; flex-direction: column; gap: 4px; }
.res-desc { font-size: 13px; color: #666; line-height: 1.4; }
.res-kps { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 2px; }
.kp-tag {
  font-size: 11px;
  background: #e8f4fd;
  color: #1a73e8;
  padding: 1px 8px;
  border-radius: 10px;
}
```

### 测试
- 浏览器手动验证：启动后端 + 前端，走完整流程，确认 Resource 页面展示描述和知识点标签

### 验收标准
- [ ] Resource 页面每条资料展示 description
- [ ] Resource 页面每条资料展示 knowledge_points 标签
- [ ] 知识点标签有区分度（蓝色小标签）
- [ ] v0.5 旧格式 resource（无新字段）不报错（前端向后兼容）

---

## v0.6-4 测试更新 + 全量回归 [ ]

### 目标
更新集成测试适配新 ResourceEntry 结构，确保全量 pytest 通过。

### 依赖
- v0.6-3（前端升级完成，整体功能就绪）

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `server/tests/integration/test_resource.py` | 断言增加 description/knowledge_points |

### 实现

test_resource.py 新增/修改测试：

```python
def test_resource_has_new_fields(self):
    """v0.6: 每条资源含 description + knowledge_points"""
    state = make_state(...)
    result = resource_node(state)
    resources = result["resources"]
    for i, r in enumerate(resources):
        assert "description" in r, f"资源 {i} 缺 description"
        assert isinstance(r["description"], str) and len(r["description"]) > 0
        assert "knowledge_points" in r, f"资源 {i} 缺 knowledge_points"
        assert isinstance(r["knowledge_points"], list)
        assert len(r["knowledge_points"]) >= 1

def test_resource_urls_are_real(self):
    """v0.6: 至少有一条 URL 来自真实搜索（http 开头且非示例域名）"""
    state = make_state(...)
    result = resource_node(state)
    urls = [r["url"] for r in result["resources"]]
    assert any(u.startswith("http") for u in urls)
```

同时移除 `test_resource_relevant_to_step` 中依赖特定关键词的断言（LLM 输出不确定），改为断言 description 非空。

### 验收标准
- [ ] `python -m pytest server/tests/ -v` 全部通过，零失败
- [ ] test_resource.py 断言覆盖 description、knowledge_points 新字段
- [ ] 无回归（所有已有测试通过）

---

## v0.6 版本验收

### 整体验收
- [ ] 全量 pytest 通过，零失败
- [ ] 浏览器手动走通全流程：输入目标 → 看计划 → 看资料（含描述和知识点标签）→ 做题 → 看反馈
- [ ] Resource Agent 返回的资料 URL 可真实访问（浏览器点击能打开）
- [ ] 搜索失败时降级不崩溃（模拟断网测试）
- [ ] demo.py 完整链路可跑通

### 不包含
- SQLite 持久化
- Chat Agent
- 题库扩展 / AI 题目生成
- 用户认证
- 知识图谱
- 搜索缓存（v0.7+）
