# v0.10 — AI 题目生成：LLM 根据步骤动态出题

> 目标：Practice 节点从固定题库改为 LLM 根据当前步骤动态出题，缓存避免重复生成。

## 现状问题

```python
# 当前：3 道固定题，不管学什么步骤都出一样
QUESTION_BANK = [
    {"id": "q1", "content": "二次函数 y=x² 的对称轴是？", ...},
    {"id": "q2", "content": "二次函数 y=(x-1)²+2 的顶点坐标是？", ...},
    {"id": "q3", "content": "二次函数 y=x²-2x+1 的判别式 Δ 的值是？", ...},
]
```

## v0.10 改动

```
Practice 节点:
  step.title + step.desc
       │
       ▼
  LLM 生成 2-3 道选择题
       │
       ▼
  {questions, waiting_for_answer: true}
       │
       ▼
  缓存到 state（repeat 时不重新生成）
```

## 子任务清单

| # | 任务 | 状态 |
|---|------|------|
| v0.10-1 | Practice Agent 重构 + AI 出题 Prompt | [x] |
| v0.10-2 | 测试更新 + 回归 | [x] |

---

## v0.10-1 Practice Agent 重构 + AI 出题 Prompt [ ]

### 目标
Practice 节点调用 LLM 根据当前步骤生成 2-3 道选择题，首次生成后缓存到 state，repeat 时复用。

### 依赖
- 无

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `server/orchestrator/agents/practice.py` | LLM 出题逻辑 + 缓存 |
| 新建 | `data/prompts/practice.md` | 出题 Prompt |

### 实现

**practice.py**：

```python
def practice_node(state: AgentState) -> dict:
    # 如果已有题目（repeat 场景），直接复用
    if state.get("questions"):
        return {"waiting_for_answer": True}

    step = state["plan"][state["current_step"]]
    prompt = render_prompt(
        "practice",
        task_goal=state["task_goal"],
        step_title=step["title"],
        step_desc=step["desc"],
    )
    result = llm_invoke_json(prompt)
    # LLM 输出: {"questions": [{"id": "q1", "content": "...",
    #   "options": ["A. ...", ...], "answer": "A", "kp": "..."}, ...]}
    return {
        "questions": result["questions"],
        "waiting_for_answer": True,
    }
```

**practice.md**：

    # Role
    你是高一数学习题设计师，擅长根据学习目标出选择题。

    # Task
    根据当前学习步骤，生成 2-3 道四选一选择题。

    # Input
    - 学习目标：{task_goal}
    - 当前步骤：{step_title}
    - 步骤描述：{step_desc}

    # Output Format
    严格输出 JSON：
    {"questions": [{"id": "q1", "content": "...",
      "options": ["A. ...", ...], "answer": "A", "kp": "..."}]}

    # Constraints
    - 2-3 道题，难度递增
    - 每道题有且仅有一个正确答案
    - 选项之间互斥且长度相近
    - 不要出超纲题

### 测试
- 集成测试：调真实 LLM，验证生成 2-3 道题，格式正确
- 缓存测试：已有 questions 时不重新生成

### 验收标准
- [ ] Practice 根据步骤生成 2-3 道题
- [ ] 每道题含 id/content/options/answer/kp
- [ ] 选项 4 个，互不相同
- [ ] repeat 时复用已有题目（不重复生成）
- [ ] demo.py 完整链路可跑通

---

## v0.10-2 测试更新 + 回归 [ ]

### 目标
更新测试适配 AI 出题，全量回归。

### 依赖
- v0.10-1

### 改动文件

| 操作 | 文件 | 说明 |
|------|------|------|
| 修改 | `server/tests/unit/test_graph_structure.py` | Mock LLM 出题 |
| 新建 | `server/tests/integration/test_practice.py` | 集成测试 |

### 验收标准
- [ ] 全量 pytest 通过
- [ ] 无回归

---

## v0.10 版本验收

### 整体验收
- [x] 全量 pytest 通过，零失败（34 单元）
- [x] Practice 根据步骤动态出题（LLM 生成）
- [x] repeat 时复用已有题目
- [x] demo.py 完整链路可跑通

### 不包含
- 题目难度自适应
- 解答题 / 填空题
- 题库持久化
